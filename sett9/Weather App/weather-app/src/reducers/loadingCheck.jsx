/* import { IS_LOADING } from "../actions/fetchDataActions";


const initialState = {
    load: true,
  };
  
  const loadingCheckReducer = (state = initialState, action) => {
    switch (action.type) {
      case IS_LOADING:
        return {
          load: action.payload
        };
    
      default:
        return {
            load: true
          };;
    }
  };
  
  export default loadingCheckReducer; */